[tox]
envlist = py310

[testenv]
deps = -rrequirements.dev.txt
commands =
    coverage erase
    coverage run -m pytest {posargs:tests}
    coverage report
    coverage xml

[pytest]
env =
    DEVELOPMENT=true
